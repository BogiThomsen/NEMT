version: '3.7'
services:
    device-service:
        build: .
        ports:
            - 5200
        volumes:
            - .:/code
        labels:
            - "traefik.frontend.rule=Host:device.localhost"